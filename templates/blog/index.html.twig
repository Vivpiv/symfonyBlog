{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}


{% block body %}
    <h1><strong>All articles from article s table : </strong></h1>

    {#{{ form_start(form) }}#}
        {#{{ form_row(form.searchField,{'label': 'contenu'}) }}#}
        {#<button type="submit" name="btnSearch">Rechercher</button>#}
    {#{{ form_end(form) }}#}

    {% for article in articles %}
        <div>
            <h5><u>{{ loop.index }} / {{ article.title }} - Category : {{ article.category.name }}</u></h5>
            <p>{{ article.content }}</p>
        </div>
    {% endfor %}
    <a href="{{ path('homepage') }}">
        Back to homepage.
    </a>

    <a href="{{ path('blog_show', { 'slug': "javascript-vs-php" }) }}">
        Testing show() method from BlogController with a real article s slug.
    </a>
    <br>
    <a href="{{ path('blog_index') }}">
        Testing index() method from BlogController to view all articles.
    </a>

{% endblock %}
